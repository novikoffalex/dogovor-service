# 🔄 Руководство по восстановлению системы

## 📦 Доступные точки восстановления

### 🏷️ **Теги (рекомендуется для восстановления):**

| Тег | Описание | Статус | Функциональность |
|-----|----------|--------|------------------|
| `v1.1.2-stable` | **Стабильная версия** | ✅ Рабочая | PDF + автоматическая проверка + исправленный счетчик |
| `v1.1.1` | Предыдущая версия | ✅ Рабочая | PDF + автоматическая проверка + исправленный счетчик |
| `v1.1.0` | Старая версия | ✅ Рабочая | PDF + автоматическая проверка через Zamzar |
| `v1.0.0` | Базовая версия | ✅ Рабочая | ManyChat JSON интеграция |
| `v1.0-working-docx` | DOCX версия | ✅ Рабочая | Только генерация DOCX |

### 🌿 **Ветки:**

| Ветка | Описание | Статус |
|-------|----------|--------|
| `main` | Продакшн ветка | ✅ Текущая |
| `feature/manychat-integration` | ManyChat интеграция | 🧪 Тестирование |
| `develop` | Ветка разработки | ✅ Активная |
| `docx-only` | Только DOCX | ✅ Рабочая |
| `backup-v1.1.0-working` | Бэкап текущей версии | ✅ Создан |

## 🚨 **Быстрое восстановление**

### Если что-то пошло не так с ManyChat интеграцией:

```bash
# 1. Откатиться к стабильной версии
git checkout v1.1.2-stable

# 2. Применить изменения
composer install --no-dev --optimize-autoloader
php artisan config:cache
php artisan route:cache

# 3. Перезапустить сервисы
php artisan queue:restart
```

### Если что-то пошло не так с v1.1.1:

```bash
# 1. Откатиться к предыдущей стабильной версии
git checkout v1.1.0

# 2. Применить изменения
composer install --no-dev --optimize-autoloader
php artisan config:cache
php artisan route:cache

# 3. Перезапустить сервисы
php artisan queue:restart
```

### Если нужно откатиться к v1.0.0:

```bash
# 1. Откатиться к стабильной версии без PDF автоматизации
git checkout v1.0.0

# 2. Применить изменения
composer install --no-dev --optimize-autoloader
php artisan config:cache
php artisan route:cache

# 3. Перезапустить сервисы
php artisan queue:restart
```

### Если нужна версия только с DOCX:

```bash
# Переключиться на DOCX версию
git checkout docx-only
composer install --no-dev --optimize-autoloader
php artisan config:cache
```

### Если нужна версия разработки:

```bash
# Переключиться на develop
git checkout develop
git pull origin develop
composer install --no-dev --optimize-autoloader
```

## 🔧 **Восстановление из бэкапа**

### Восстановить текущую рабочую версию v1.1.1:

```bash
# 1. Переключиться на стабильную версию
git checkout v1.1.1

# 2. Обновить зависимости
composer install --no-dev --optimize-autoloader

# 3. Очистить кеш
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 4. Перезапустить scheduler
php artisan queue:restart
```

### Восстановить из бэкап ветки:

```bash
# 1. Переключиться на бэкап ветку
git checkout backup-v1.1.0-working

# 2. Обновить зависимости
composer install --no-dev --optimize-autoloader

# 3. Очистить кеш
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 4. Перезапустить scheduler (если нужно)
php artisan queue:restart
```

### Восстановить из конкретного коммита:

```bash
# Найти нужный коммит
git log --oneline -10

# Восстановить из коммита
git checkout <commit-hash>
composer install --no-dev --optimize-autoloader
php artisan config:cache
```

## 📊 **Проверка состояния системы**

### После восстановления проверить:

```bash
# 1. Статус Git
git status
git log --oneline -3

# 2. Работа приложения
php artisan route:list | grep contract

# 3. Зависимости
composer check-platform-reqs

# 4. Кеш
php artisan config:show app.url
```

### Проверить функциональность:

```bash
# 1. Генерация договора
curl -X POST http://localhost:8000/api/contract/generate \
  -H "Content-Type: application/json" \
  -d '{"client_full_name":"Test","inn":"123","client_address":"Test","bank_name":"Test","bank_account":"123","bank_bik":"123"}'

# 2. Проверка PDF статуса (только для v1.1.0)
php artisan zamzar:check-status

# 3. Проверка scheduler (только для v1.1.0)
php artisan schedule:list
```

## ⚠️ **Важные моменты**

### При откате с v1.1.0 на v1.0.0:

1. **Удалить cron job** для zamzar:check-status
2. **Обновить webhook в Zamzar** (если нужно)
3. **Проверить API endpoints** - некоторые могут не работать

### При откате на docx-only:

1. **PDF функциональность** будет недоступна
2. **Scheduler** не нужен
3. **Zamzar интеграция** отключена

## 🆘 **Экстренное восстановление**

### Если система полностью сломана:

```bash
# 1. Полный откат к последней рабочей версии
git reset --hard v1.0.0
git clean -fd

# 2. Переустановить зависимости
rm -rf vendor/
composer install --no-dev --optimize-autoloader

# 3. Очистить все кеши
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

# 4. Восстановить конфигурацию
php artisan config:cache
php artisan route:cache
```

## 📞 **Контакты для поддержки**

При проблемах с восстановлением:
1. Проверить логи: `tail -f storage/logs/laravel.log`
2. Проверить статус Git: `git status`
3. Проверить права доступа: `ls -la storage/`
4. Обратиться к разработчику
