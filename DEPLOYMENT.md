# Инструкция по деплою версии v1.1.0

## 🚀 Что нового в v1.1.0

- ✅ **Автоматическая проверка PDF статуса** через Zamzar API
- ✅ **Новые API endpoints** для проверки и скачивания PDF
- ✅ **Команда `zamzar:check-status`** для автоматической проверки
- ✅ **Scheduler** для выполнения команды каждые 30 секунд
- ✅ **Улучшенный фронтенд** с автоматическим обновлением ссылки на PDF

## 📋 Шаги деплоя

### 1. Обновление кода на сервере

```bash
# Подключиться к серверу
ssh your-server

# Перейти в директорию проекта
cd /path/to/your/project

# Получить последнюю версию
git pull origin main
git checkout v1.1.0

# Обновить зависимости
composer install --no-dev --optimize-autoloader

# Очистить кеш
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan queue:restart
```

### 2. Настройка cron job для автоматической проверки PDF

```bash
# Открыть crontab
crontab -e

# Добавить строку (замените путь на ваш):
* * * * * cd /path/to/your/project && php artisan schedule:run >> /dev/null 2>&1
```

### 3. Настройка webhook в Zamzar

1. Зайти в дашборд Zamzar: https://developers.zamzar.com/user/dashboard
2. Перейти в **Integrations** → **Webhooks**
3. Обновить URL на: `https://yourdomain.com/api/zamzar/webhook`
4. Сохранить настройки

### 4. Проверка работы

```bash
# Проверить команду проверки статуса
php artisan zamzar:check-status

# Проверить расписание
php artisan schedule:list

# Проверить логи
tail -f storage/logs/laravel.log
```

## 🔧 Новые API endpoints

- `GET /api/contract/check-pdf-status/{filename}` - проверка статуса PDF
- `GET /api/contract/download-pdf/{filename}` - скачивание PDF файла

## 📊 Мониторинг

### Логи для отслеживания:
```bash
# Логи генерации договоров
grep "Contract generated" storage/logs/laravel.log

# Логи Zamzar конвертации
grep "Zamzar" storage/logs/laravel.log

# Логи ошибок
grep "ERROR" storage/logs/laravel.log
```

### Проверка статуса задач:
```bash
# Посмотреть все задачи Zamzar
cat storage/app/zamzar_jobs.json | jq .
```

## ⚠️ Важные моменты

1. **Webhook URL** должен быть доступен из интернета
2. **SSL сертификат** должен быть валидным для webhook
3. **Cron job** должен работать для автоматической проверки
4. **Права доступа** к папке `storage/app/contracts/` должны быть корректными

## 🐛 Отладка

### Если PDF не генерируется:
```bash
# Проверить статус задач
php artisan zamzar:check-status

# Проверить API ключ Zamzar
curl -H "Authorization: Bearer YOUR_API_KEY" "https://api.zamzar.com/v1/jobs"

# Проверить webhook
curl -X POST https://yourdomain.com/api/zamzar/webhook -d '{"test": "data"}'
```

### Если scheduler не работает:
```bash
# Проверить cron
crontab -l

# Запустить вручную
php artisan schedule:run

# Проверить права на файлы
ls -la artisan
```

## 📞 Поддержка

При возникновении проблем:
1. Проверить логи: `tail -f storage/logs/laravel.log`
2. Проверить статус задач: `php artisan zamzar:check-status`
3. Проверить webhook в дашборде Zamzar
4. Убедиться, что cron job работает
