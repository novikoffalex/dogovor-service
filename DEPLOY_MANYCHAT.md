# 🚀 Быстрый деплой ManyChat интеграции

## ⚠️ ВНИМАНИЕ: Система в продакшене!

### 📋 Подготовка (уже выполнено)
- ✅ Создан тег `v1.1.2-stable` для отката
- ✅ ManyChat функциональность в ветке `feature/manychat-integration`
- ✅ Все изменения протестированы локально

### 🎯 Команды для деплоя на сервер

```bash
# 1. Подключиться к серверу
ssh root@dogovor-service-main-srtt1t.laravel.cloud

# 2. Перейти в директорию проекта
cd /var/www/html

# 3. Создать бэкап текущей версии
cp -r . ../backup-$(date +%Y%m%d-%H%M%S)

# 4. Получить обновления
git fetch origin

# 5. Переключиться на ветку ManyChat
git checkout feature/manychat-integration

# 6. Обновить зависимости
composer install --no-dev --optimize-autoloader

# 7. Очистить кэш
php artisan config:clear
php artisan route:clear
php artisan view:clear

# 8. Проверить новые маршруты
php artisan route:list | grep -E "(upload-signed|download-signed|send-to-manychat)"
```

### ✅ Проверка работоспособности

```bash
# 1. Проверить веб-интерфейс
# Открыть https://dogovor-service-main-srtt1t.laravel.cloud/contract

# 2. Протестировать загрузку подписанного договора
# Использовать кнопку "Загрузить подписанный договор"

# 3. Проверить API (опционально)
curl -X POST https://dogovor-service-main-srtt1t.laravel.cloud/api/contract/upload-signed \
  -F "signed_contract=@test.pdf" \
  -F "contract_number=TEST-001" \
  -H "Accept: application/json"
```

### 🔄 Если все работает - мерж в main

```bash
# 1. Переключиться на main
git checkout main

# 2. Мерж ManyChat функциональности
git merge feature/manychat-integration

# 3. Отправить в репозиторий
git push origin main

# 4. Создать тег новой версии
git tag v1.2.0-manychat
git push origin v1.2.0-manychat
```

### 🚨 Если что-то пошло не так - откат

```bash
# Быстрый откат к стабильной версии
git checkout v1.1.2-stable

# Очистить кэш
php artisan config:clear
php artisan route:clear
php artisan view:clear

# Перезапустить сервисы
php artisan queue:restart
```

### 📊 Что добавлено

**Новые API endpoints:**
- `POST /api/contract/upload-signed` - загрузка подписанного договора
- `GET /api/contract/download-signed/{filename}` - скачивание подписанного договора  
- `POST /api/contract/send-to-manychat` - отправка ссылки в ManyChat

**Обновленный фронтенд:**
- Показывает информацию о ManyChat после загрузки
- Отображает поле `pdf_scan_url` и ссылку для ManyChat

### 🎯 Логика работы

1. Клиент загружает скан подписанного договора
2. Система сохраняет файл и генерирует публичную ссылку
3. Фронтенд показывает данные для ManyChat:
   - Поле: `pdf_scan_url`
   - Значение: ссылка на файл
4. Сотрудник может использовать эту ссылку в ManyChat
5. Сотрудник может скачать файл по ссылке

### 📞 Поддержка

При проблемах:
1. Проверить логи: `tail -f storage/logs/laravel.log`
2. Проверить статус Git: `git status`
3. Откатиться к `v1.1.2-stable`

